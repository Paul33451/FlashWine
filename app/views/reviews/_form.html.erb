<%= simple_form_for([ wine, review ]) do |f| %>
      <%= f.input :rating, as: :select, label: "Note", collection: ["1", "2", "3", "4", "5"] %>
      <%= f.input :content, label: "Commentaire" %>
  <%= f.button :submit, value: "Valider", class: "btn btn-flat btn-review" %>
<% end %>
