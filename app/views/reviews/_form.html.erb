<%= simple_form_for([ wine, review ]) do |f| %>
  <%= f.input :rating, label: "Note"  %>
  <%= f.input :content, label: "Commentaire" %>
  <%= f.button :submit, label: "VALIDER", class: "btn-flat" %>
<% end %>
